# BoredApp Server — moved

> **This repository has been consolidated into the BoredApp monorepo:
> https://github.com/reannu123/BoredApp**
>
> The server now lives under [`server/`](https://github.com/reannu123/BoredApp/tree/master/server)
> in that repo, alongside the client, with Docker and a single setup guide.
> This repository is archived (read-only) and kept only for history.

---

This was the Express API + auth server for the BoredApp client. See the
[monorepo README](https://github.com/reannu123/BoredApp#readme) for setup,
architecture, and how to run the full stack with `docker compose up`.
